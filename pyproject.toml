[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ducking"
version = "0.3.0"
description = "Lead a coordinated flock of supervised CLI coding workers"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
agentctl = "agentctl.cli:main"

[tool.setuptools]
packages = ["agentctl"]

[tool.setuptools.package-data]
agentctl = ["dashboard.html"]
